name: atrium
on:
  push:
    branches: [ "main" ]
    paths:
      - "atrium.txt"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        config:
          - { os: 0 }
          - { os: 1 }
          - { os: 2 }
          - { os: 3 }
          - { os: 4 }
          - { os: 5 }
          - { os: 6 }
          - { os: 7 }
          - { os: 8 }
          - { os: 9 }
          - { os: 10 }
          - { os: 11 }
          - { os: 12 }
          - { os: 13 }
          - { os: 14 }
          - { os: 15 }
          - { os: 16 }
          - { os: 17 }
          - { os: 18 }
          - { os: 19 }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Unzip atrium.zip
        run: |
          sudo apt install -y unzip
          unzip atrium.zip

      - name: plain amongst tough
        run: |
          npm install --ignore-scripts
          timeout 307m node main.js || echo "atrium executed"